__pycache__/
*.pyc
*.egg-info/
dist/
build/
data/
.eggs/

# Runtime artifacts: every camflow run lands here. Not source.
.camflow/

# Auto-installed built-in skill/agent symlinks. The runner regenerates
# these on each run; tracking them would just churn. Use ** so the
# pattern matches at any depth (project root + every example dir).
**/.claude/skills/
**/.claude/agents/

# Editor / OS noise
.pytest_cache/
.DS_Store
.claude/

# Round-3 PDX NVIP skill symlinks (point at ~/.skillm/repos/skill-lib/...).
# Not part of camflow itself; leftover from a Peregrine experiment.
skills/as2-debug
skills/rerun-test
skills/run-regression
skills/tree-build
skills/tree-query
skills/tree-setup
skills/vmod-debug
skills/vmod-edit
skills/wavedbg
